{
  "name": "economybackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "nodemon --ignore 'data.json' --ignore 'backup' --ignore 'log.json'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^1.18.10",
    "ws": "^6.2.2"
  }
}
